{
    "PermissionsToRoleCreation": {
        "operationId": "createRole",
        "requestBody": {
            "permissions": "$response.body#/data/data[*].id",
            "description": "The permission IDs returned in the response can be used when creating a role."
        },
        "description": "Link to attach permission IDs returned in the response to a role during creation."
    }
}