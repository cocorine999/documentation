{
    "DepartementRequest": {
        "required": [
            "name"
        ],
        "type": "object",
        "properties": {
            "name": {
                "type": "string",
                "example": "{{$randomDepartment}}",
                "default": "Production",
                "description": "Name of the departement"
            }
        }
    }
}