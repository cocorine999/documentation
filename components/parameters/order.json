{
    "name": "order",
    "in": "query",
    "description": "Ordering",
    "required": false,
    "schema": {
        "enum": ["asc", "desc"],
        "default": "desc",
        "example": "desc"
    }
}